{
    "name": "music-grab-standalone",
    "version": "2.0.0",
    "main": "index.js",
    "scripts": {
        "start": "node index.js",
        "build": "npm install && cd frontend && npm install && npm run build && cp -r dist ../"
    },
    "dependencies": {
        "express": "^4.18.2",
        "cors": "^2.8.5",
        "yt-search": "^2.13.1",
        "ffmpeg-static": "^5.3.0"
    }
}